/* Basic global reset or box-sizing (optional) */
* {
box-sizing: border-box;
margin: 0;
padding: 0;
}

/* Overall page styling */
.homepage {
font-family: Arial, sans-serif;
color: #333;
}

/* Hero Section */
.hero-section {
text-align: center;
padding: 60px 20px;
max-width: 1200px;
margin: 0 auto;
}

.hero-title {
font-size: 3rem;
font-weight: bold;
margin-bottom: 20px;
line-height: 1.2;
}

.hero-subtext {
font-size: 1.2rem;
margin: 0 auto 40px;
max-width: 800px;
}

/* "Start Shopping" button */
.hero-button {
background-color: #ffc107; /* Accent color */
border: none;
padding: 15px 30px;
font-size: 1rem;
cursor: pointer;
border-radius: 4px;
transition: background-color 0.2s ease;
}

.hero-button:hover {
background-color: #e0a800; /* Slightly darker on hover */
}

/* Container for the button & "Join Us" text side by side */
.hero-cta {
display: flex;           /* Place the button and text side by side */
align-items: flex-start; /* Align them at the top edge */
justify-content: center; /* Center both horizontally in the hero section */
gap: 30px;               /* Space between button and text */
margin-top: 20px;
flex-wrap: wrap;         /* Allow wrapping on smaller screens */
}

/* "Join Us" text block next to the button */
.join-us {
max-width: 400px;        /* Keep the text area from getting too wide */
text-align: left;        /* Align text to the left */
}

.join-us h3 {
margin-bottom: 10px;
font-size: 1.2rem;
}

.join-us p {
font-size: 1rem;
line-height: 1.5;
color: #555;
}

/* Benefits Section */
.benefits-section {
display: flex;
justify-content: center;
flex-wrap: wrap;
gap: 40px;
padding: 40px 20px;
max-width: 1200px;
margin: 0 auto;
}

.benefit-card {
text-align: center;
width: 200px;
}

.benefit-icon {
font-size: 2.5rem;
margin-bottom: 10px;
}

.benefit-title {
font-size: 1.2rem;
font-weight: bold;
margin-bottom: 5px;
}

.benefit-description {
font-size: 0.9rem;
line-height: 1.4;
color: #555;
}

/* Compliance / Trust Badges */
.compliance-section {
text-align: center;
padding: 20px;
max-width: 800px;
margin: 0 auto;
}

.compliance-text {
font-size: 1rem;
color: #555;
}
